---
name: Feature / compatibility request
about: Propose a feature or report a missing compatibility surface
labels: enhancement
---

**Problem / use case**

**Proposed behavior**

**Compatibility context** (which client, framework or API is affected — e.g. "LangChain ElasticsearchStore uses `_search` with `knn` + `query`")

**Alternatives considered**
